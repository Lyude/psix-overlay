diff --git a/vboxdrv/r0drv/linux/memobj-r0drv-linux.c b/vboxdrv/r0drv/linux/memobj-r0drv-linux.c
index b0b2146..f5dd6a7 100644
--- a/vboxdrv/r0drv/linux/memobj-r0drv-linux.c
+++ b/vboxdrv/r0drv/linux/memobj-r0drv-linux.c
@@ -1465,7 +1465,7 @@ DECLHIDDEN(int) rtR0MemObjNativeMapUser(PPRTR0MEMOBJINTERNAL ppMem, RTR0MEMOBJ p
 
 #if    LINUX_VERSION_CODE >= KERNEL_VERSION(3, 7, 0)
                     vma->vm_flags |= VM_DONTEXPAND | VM_DONTDUMP;
 #else
-                    vma->vm_flags |= VM_RESERVED;
+                    vma->vm_flags |= VM_DONTEXPAND | VM_DONTDUMP; /* These flags help making 100% sure some bad stuff wont happen (swap, core, ++). */
 #endif
 #elif LINUX_VERSION_CODE >= KERNEL_VERSION(2, 6, 11)
                     rc = remap_pfn_range(vma, ulAddrCur, page_to_pfn(pMemLnxToMap->apPages[iPage]), PAGE_SIZE, fPg);
 #elif defined(VBOX_USE_PAE_HACK)
